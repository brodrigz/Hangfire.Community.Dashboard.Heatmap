namespace TestApp;

public static class TestJobs
{
    public static void NoOp(string jobName)
    {
        // Intentionally empty. The sample only needs recurring schedules for the heatmap.
    }
}
